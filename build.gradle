group = 'cn.thinkjoy.qtonebi'
version = '0.0.1-SNAPSHOT'

apply plugin: 'war'

repositories {
    mavenCentral()
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}

task installZip(type: Zip) {
    from('./work')
    into('./uc-webfront/')
}

